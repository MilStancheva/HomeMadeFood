@model DateTime?
@{
    var value = "";
    if (Model.HasValue)
    {
        value = string.Format("{0:d}", Model.Value.ToString("dd/MM/yyyy"));
    }
}
@Html.TextBox("", value, new { @class = "datefield", type = "text", name = "selectedDate" })